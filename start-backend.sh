#!/usr/bin/env bash
set -e
cd "$(dirname "$0")"
echo "QueueKisan hosted mode uses Supabase + Firebase directly. No local backend or seed step is required."
echo "Start the role apps with ./start-admin.sh, ./start-counter.sh, and ./start-farmer.sh"
